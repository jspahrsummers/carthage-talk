This is a demo project created for the presentation, intended to demonstrate how
to install Carthage dependencies into an application.

To see Carthage in action, simply install it:

```sh
brew update
brew install carthage
```

then bootstrap the demo:

```sh
cd CarthageDemo
carthage bootstrap
```
